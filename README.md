# RentalX API

Project built with node.js and express. This work has been done during Ignite classes, provided by Rocketseat

To run:

1. Clone the project
2. Run `npm install` or `yarn`
3. Run `npm run dev` or `yarn dev` to start in development mode

happy coding!
